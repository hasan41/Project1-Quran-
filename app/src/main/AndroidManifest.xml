<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hasannaseer.quranapp2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity2"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".SignupActivity" />

        <!--
            <activity android:name="com.example.hasannaseer.quranapp2.SignupActivity"
                  android:configChanges="orientation|screenSize"/>
        -->

        <activity
            android:name=".SurahSelectionView"
            android:theme="@style/AppTheme.SurahSelection" />
        <activity
            android:name=".Aya_selection_view"
            android:theme="@style/AppTheme.SurahSelection" />
        <activity android:name=".MainActivity" />
        <!--
        <activity android:name=".LoginActivity2" />
        <activity android:name=".SignupActivity"> </activity>
        -->
        <activity android:name=".menubar"></activity>
    </application>

</manifest>